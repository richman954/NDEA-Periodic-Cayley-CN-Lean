# Experiment 003 Step 4 — Pre-closure verified snapshot

Timestamp: 2026-09-07T16:27:58-04:00

The exact finite-`N` telescoping identity and global induced operator-norm
bound have compiled successfully in an independent combined run and a bounded,
serial, module-by-module RAM-backed run. The qualifying modular run compiled
the actual immutable Step 2 and Step 3 modules, the actual Step 4 production
module, and the actual Step 4 controls as separate modules, then emitted their
exact signatures and clean axiom audits.

The compiled controls establish the genuine non-unitary amplification witness.
They also formally prove that a linear-in-`N` upper bound entails the weaker
`N^2` upper bound for `N >= 1`, and separately certify strict quadratic slack
at the exact small local defect `1/10`.

The required Step 3 commit and annotated tag remain intact, its separate
worktree is clean, and protected predecessor sources, evidence, release data,
and project manifests have zero diff. No forbidden proof mechanism is present.

This is a historical pre-closure snapshot. All mathematical checks are
verified, but the final checksum manifest, release commit/tag, and external
release bundle have not yet been created.

Only Experiment 003 Step 4 was executed. The Step 5 continuous-exponential
project was not started.
