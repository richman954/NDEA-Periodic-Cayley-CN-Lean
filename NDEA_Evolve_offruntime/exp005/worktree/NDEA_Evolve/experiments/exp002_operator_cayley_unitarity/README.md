# Experiment 002 evidence index

This directory is the auditable Julia → Lean workspace for the frozen
noncommutative Cayley-composition mission.

Current in-tree status: **ALL FROZEN MATHEMATICAL AND ASSURANCE GATES GREEN;
OUTER DELIVERY RECEIPT REQUIRED**. Exact Julia discovery/certificate validation,
the universal Lean core, the strengthened positive witness, the seven-case
rejection controls, the signature/axiom and forbidden-token audits, and the
cache-reusing verbose root build are green. The immutable package cannot attest
its own outer archive; final local-delivery status is recorded only by the
adjacent off-runtime receipt and closure report. See `final_evaluation_report.md`
for the precise evidence categories and scope.

Core evidence:

- `experiment_goal.md`, `scope_and_success_criteria.md`, `recovery_policy.md` —
  frozen mission and constraints;
- `mathematical_derivation.md` — transparent algebra and trust boundary;
- `source/julia/` — exact/numerical Julia discovery source;
- `certificates/operator_cayley_core.json` — machine-readable Julia artifact;
- `validator/` and `controls/fixtures/` — independent strict validator and
  altered-certificate regression fixtures;
- `../../NDEAEvolve/Experiments/Exp002/OperatorCayley.lean` — universal Lean
  formalization;
- `../../NDEAEvolve/Experiments/Exp002/AdversarialWitnesses.lean` — compiling
  exact positive/counterexample witnesses;
- `controls/lean/` — isolated deliberately false Lean units and their runner;
- `assurance/` — production-source scan and declaration/axiom audit;
- `logs/`, `metadata/`, and `recovery/` — commands, outputs, exit codes,
  failures, repairs, and runtime-loss provenance.

Milestone integrity files are layered: the existing preflight and Julia-green
manifests remain immutable. Each historical checkpoint manifest retains its
original scope; the final manifest must cover every regular file selected for
the final package except the manifest itself. A result is green only when its
corresponding state/receipt says so explicitly.

Experiment 001 is reference-only and remains immutable. External human review
of Experiment 002 is pending.
